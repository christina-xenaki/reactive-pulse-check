// Responsibility: building the exportable record (SPEC.md section J) —
// copy for email, copy for Slack, and print — from the current result, and
// collecting the optional decision-maker fields at export time only.
